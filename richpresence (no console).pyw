import richpresence
import time

# imports the main richpresence.py and runs the main_script() function, done this way to not increase the file size by the same size as the main file.

if __name__ == "main":
        richpresence.main_script()


while True:
        time.sleep(1)